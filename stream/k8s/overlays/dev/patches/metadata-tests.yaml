apiVersion: apps/v1
kind: Deployment
metadata:
  name: metadata-service
spec:
  template:
    spec:
      containers:
      - name: app
        volumeMounts:
        - name: tests
          mountPath: /tests
          readOnly: true
      volumes:
      - name: tests
        hostPath:
          path: /tests
          type: Directory
