# TODO: 本番（外部DB/外部MinIO等）へ移行する際は、本ファイルに実際の秘密情報を定義し、sops を利用して暗号化してください。
# 暗号化された本ファイルを参照するように、親ディレクトリの secrets.yaml も修正する必要があります。
# 
# 現状は、一旦現状のNFSデータで動かせるか検証するため、
# secrets.yaml にテスト用のID/PWがハードコードされています。

# MONGO_INITDB_ROOT_USERNAME: pechka
# MONGO_INITDB_ROOT_PASSWORD: changeme
# MONGO_INITDB_DATABASE: pechka
# ... (将来用の設定値をここに定義してsopsで暗号化する)
