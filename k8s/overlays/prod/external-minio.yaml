# nuage-cluster リポジトリの external-service namespace が実IPを一元管理する。
# ここでは同名serviceでDNS転送するだけなので、IPをこのリポジトリ側で持つ必要はない。
apiVersion: v1
kind: Service
metadata:
  name: minio
  namespace: pechka
spec:
  type: ExternalName
  externalName: minio.external-service.svc.cluster.local
